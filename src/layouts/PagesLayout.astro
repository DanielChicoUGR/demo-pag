---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import { Image } from 'astro:assets';

import type { ImageMetadata } from 'astro';
import NavComponent from 'src/components/NavComponent.astro';

interface PagesLayoutProps {
  title: string;
  description: string;
  imagePath: string;
  altText: string;
}

const { title, description, imagePath, altText } = Astro.props as PagesLayoutProps;

const images = import.meta.glob<{ default: ImageMetadata }>(
  '../assets/images/*.{jpeg,jpg,png,gif}'
);
if (!images[imagePath])
  throw new Error(
    `"${imagePath}" does not exist in folder: "src/assets/images/*.{jpeg,jpg,png,gif}"`
  );
---

<html lang="en">
  <head>
    <BaseHead title={title} description={description} />
  </head>

  <body>
    
    <div class="header-container">
      <div class="image_filter">
        <Image src={images[imagePath]()} alt={altText} class="background-image" />
      </div>

      <div class="header-text">
        <NavComponent class="nav-component" />
        <slot name="top"/>
      </div>
      <main>
        <slot name="body" />
      </main>
      <Footer />
    </div>
  </body><style>
    .header-text {
      align-items: center;
      width: 100%;
      display: flex;
      flex-direction: column;
      position: absolute;
      top: 0%;
      color: white;
      margin-left: auto;
      margin-right: auto;

    }

    
    .header-container {
      position: relative;
      width: 100%;
      height: 70vh; /* Ajusta esto según tus necesidades */
    }
    .image_filter::after {
      content: '';
      background: rgba(0, 0, 0, 0.3);
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
    }
    .background-image {
      width: 100%;
      object-fit: cover;
      height: 70vh;
    }
  </style>
</html>
